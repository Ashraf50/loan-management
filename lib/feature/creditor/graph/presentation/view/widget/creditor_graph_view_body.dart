import 'package:flutter/material.dart';

class CreditorGraphViewBody extends StatelessWidget {
  const CreditorGraphViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}